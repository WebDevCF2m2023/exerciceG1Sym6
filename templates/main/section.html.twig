{% extends 'template.html.twig' %}

    {% block title %}
        {{ parent() }} Section
    {% endblock %}

{% block mainBody %}
    {% for section in sections %}
        <p>{{ section.sectionTitle }}</p>
    {% endfor %}
{% endblock %}